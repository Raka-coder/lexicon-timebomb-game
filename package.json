{
  "dependencies": {
    "@tanstack/react-query": "^5.100.14"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "3.8.3"
  },
  "scripts": {
    "prepare": "husky",
    "docker:dev": "docker compose up --build",
    "docker:dev:d": "docker compose up --build -d",
    "docker:down": "docker compose down",
    "podman:dev": "podman-compose up --build",
    "podman:dev:d": "podman-compose up --build -d",
    "podman:down": "podman-compose down"
  }
}
